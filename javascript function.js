function calculateSum(a, b) {
    var sum = a + b;
    console.log('The sum is: ' + sum);
}


/*

Review Points:

Function only logs the result instead of returning it.
Usage of var instead of let or const.

*/
